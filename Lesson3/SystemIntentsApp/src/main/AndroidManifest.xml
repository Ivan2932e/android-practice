<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ivan_filatov.systemintentsapp">

    <!-- Разрешение для звонков (для ACTION_DIAL не обязательно, но добавим) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Разрешение для доступа к интернету (для браузера) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Добавляем тег uses-feature для телефонии -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RumireaFilatovlesson3">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>